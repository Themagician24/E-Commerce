{% extends 'base.html.twig' %}
{% block body %}

    <div class="container my-5">
        <h1> Espace membre 💻 </h1>

        <div class="row">
            <div class="col-md-3">
                {% include 'account/menu.html.twig' %}
            </div>
            <div class="col-md-9 ">
                <div class="d-flex justify-content-between">
                    <h5 >Mes adresses</h5>
                    <a href="{{ path('app_account_address_form') }}"
                       class="btn btn-bd-primary ">
                        Ajouter une adresse
                    </a>
                </div>
                {% if app.user.adresses|length== 0 %}
                    <p>Vous n'avez pas encore enregistrer d'adresse sur la Boutique Africaine</p>
                {% else %}

                <div class="row">
                    {% for address in app.user.adresses %}
                        <div class="col-md-5 mt-2">

                            <div class="bg-light p-2 text-center">
                               <strong> {{ address.firstname }} {{ address.lastname }}</strong> <br>
                                {{ address.adresse }} <br>
                                {{ address.postal }} {{ address.city }} <br>
                                {{ address.country }} <br>
                                <a href="{{ path('app_account_address_form', { id : address.id }) }}" class="small text-info fw-bold text-decoration-none">Éditer</a>
                                <a href="{{ path('app_account_address_delete', { id : address.id }) }}" class="small text-danger fw-bold text-decoration-none">Supprimer</a>

                            </div>
                        </div>

                    {% endfor %}
                </div>
                {% endif %}



            </div>
        </div>

    </div>

{% endblock %}